import { Link } from "react-router-dom";

const Footer = () => {
    return (
        
        <footer className="p-4 bg-white flex items-center justify-between dark:bg-gray-900">
            <span className="text-sm text-gray-500 text-center dark:text-gray-400">
                Human Resources
            </span>
        </footer>

    );
}

export default Footer;